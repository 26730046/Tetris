\section{Kiến trúc hệ thống và Công nghệ sử dụng}

\subsection{Môi trường phát triển và Thư viện}
% [Gợi ý nội dung: Ngôn ngữ C/C++ (C++11/C++17), trình biên dịch GCC / MSVC, thư viện chuẩn iostream, conio.h / ncurses, thời gian time.h / ctime...]
\textit{[Nội dung bổ sung sau: Chi tiết môi trường phát triển, IDE sử dụng (VS Code, CLion, Visual Studio) và các thư viện liên quan]}

\subsection{Sơ đồ kiến trúc tổng quan (Architecture Overview)}
% [Gợi ý nội dung: Mô hình vòng lặp trò chơi (Game Loop: Input -> Update State -> Check Collision -> Render -> Sleep/Delay)]
\textit{[Nội dung bổ sung sau: Sơ đồ khối kiến trúc và giải thích quy trình xử lý]}

\subsection{Luồng hoạt động chính của Game Loop}
% [Gợi ý nội dung: Trình bày chi tiết luồng while(1) trong hàm main, cơ chế bắt phím không chặn (non-blocking kbhit), cập nhật tọa độ khối gạch, xử lý va chạm và render lại màn hình]
\textit{[Nội dung bổ sung sau: Mô tả chi tiết sơ đồ luồng dữ liệu (Data Flow) và Game Loop]}
